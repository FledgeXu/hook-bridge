# AGENTS.md

Update the AGENTS.md index after every task is completed.

## Fundamental Principle
For any updates to AGENTS.md, only the sections under Maintain by Robot should be updated; all other parts must not be modified.

## Maintain by Human
- Do clean code.
- As little code as possible.
- The code should be elegant and abstract.
- Don't touch .codex Cargo.toml.

## Maintain by Robot
- Implemented `impl-docs/hook-bridge-implementation/01-project-bootstrap-and-test-harness.md` project bootstrap, CLI skeleton, runtime side-effect abstractions, unified error model, smoke tests, and coverage gate command.
- Static-check and CLI regression fixes (file index): `src/main.rs`, `src/lib.rs`, `src/app.rs`, `src/generate/mod.rs`, `src/run/mod.rs`, `src/runtime/fs.rs`, `src/runtime/io.rs`, `src/runtime/process.rs`, `tests/cli_generate.rs`, `tests/cli_run.rs`, `tests/cli_meta.rs`.
- Full implementation pass (file index): `src/config/mod.rs`, `src/platform/mod.rs`, `src/platform/capability.rs`, `src/platform/claude.rs`, `src/platform/codex.rs`, `src/generate/mod.rs`, `src/run/mod.rs`, `src/runtime/mod.rs`, `src/runtime/fs.rs`, `src/runtime/io.rs`, `src/runtime/process.rs`, `src/cli.rs`, `src/app.rs`, `tests/cli_generate.rs`, `tests/cli_run.rs`, `Makefile`.
- Retry/safety/runtime fixes (file index): `src/run/mod.rs`, `src/config/mod.rs`, `tests/cli_run.rs`.
- Process pipe-drain and clippy-gate fixes (file index): `src/runtime/process.rs`, `tests/cli_run.rs`.
- Stdin EOF and atomic temp-file race fixes (file index): `src/runtime/process.rs`, `src/runtime/fs.rs`, `AGENTS.md`.
- Runtime error propagation and temp-dir abstraction fixes (file index): `src/run/mod.rs`, `src/runtime/mod.rs`, `src/app.rs`, `AGENTS.md`.
- Absolute source-config and honest coverage gate fixes (file index): `src/generate/mod.rs`, `src/run/mod.rs`, `tests/cli_run.rs`, `Makefile`, `AGENTS.md`.
- Protocol-output-on-process-failure and managed-version-check fixes (file index): `src/run/mod.rs`, `src/generate/mod.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Child cleanup and exists-error-semantics fixes (file index): `src/runtime/process.rs`, `src/runtime/fs.rs`, `AGENTS.md`.
- Coverage-gate alignment fix (file index): `Makefile`, `README.md`, `AGENTS.md`.
- Retry-state cross-project isolation fix (file index): `src/run/mod.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Protocol field compatibility and gate rollback fixes (file index): `src/platform/codex.rs`, `src/platform/claude.rs`, `src/run/mod.rs`, `tests/cli_run.rs`, `Makefile`, `README.md`, `AGENTS.md`.
- Config model and YAML validation implementation for phase 02 (file index): `src/config/mod.rs`, `AGENTS.md`.
- Static-check follow-up fixes: clippy `never_loop`/test-lint cleanup, top-level `enabled` rejection test, and AGENTS constraint alignment (file index): `src/config/mod.rs`, `AGENTS.md`.
- Phase 03 platform capability matrix and generation model implementation: structured event/decision capability table, platform-generation input expansion, timeout-field mapping in generated hooks, and phase-03 coverage tests (file index): `src/platform/capability.rs`, `src/generate/mod.rs`, `AGENTS.md`.
- Static-check and protocol fix pass: clippy `map_unwrap_or` cleanup plus `platforms.<platform>.enabled` internal-control-field stripping from generated native hooks, with regression test coverage (file index): `src/platform/capability.rs`, `src/config/mod.rs`, `src/generate/mod.rs`, `AGENTS.md`.
- Commit task: packaged phase-03 capability/generation updates and protocol-field cleanup into a single changeset (file index): `src/platform/capability.rs`, `src/generate/mod.rs`, `src/config/mod.rs`, `AGENTS.md`.
- Hook static-check gate hardening: removed one-time stop-hook bypass so failing checks cannot auto-pass on the next stop, and aligned coverage check display name with actual command args (file index): `.codex/hooks/static_checks.py`, `.codex/hooks/static_checks.json`, `AGENTS.md`.
- Commit task: packaged hook static-check gate hardening and hook-config cleanup into a single changeset (file index): `.codex/hooks.json`, `.codex/hooks/static_checks.py`, `.codex/hooks/static_checks.json`, `AGENTS.md`.
- Static-check and coverage-gate repair pass: restored `coverage-gate` Makefile compatibility, removed clippy-failing test patterns, added targeted coverage for config/generate/run/runtime/platform/main error paths, and brought line coverage back above the 95% gate (file index): `src/lib.rs`, `src/config/mod.rs`, `src/generate/mod.rs`, `src/platform/capability.rs`, `src/platform/claude.rs`, `src/platform/codex.rs`, `src/run/mod.rs`, `src/runtime/fs.rs`, `src/runtime/io.rs`, `src/runtime/process.rs`, `tests/cli_meta.rs`, `Makefile`, `AGENTS.md`.
- Stop-hook static-check follow-up: removed real-stdin test dependency, replaced abort-based test helpers and setup paths with diagnosable assertions/early returns, split oversized metadata test helper coverage, and formatted the updated test modules to satisfy `cargo fmt --check` and `clippy` (file index): `src/config/mod.rs`, `src/generate/mod.rs`, `src/run/mod.rs`, `src/runtime/fs.rs`, `src/runtime/io.rs`, `src/runtime/process.rs`, `AGENTS.md`.
- Code review task: reviewed unstaged changes and reported test hermeticity and failure-diagnostics issues (file index): `AGENTS.md`.
- Code review task: reviewed current unstaged changes, reran `cargo test --quiet`, and found no blocking issues (file index): `AGENTS.md`.
- Phase 04 generate-command and managed-file-writing implementation: switched generated Claude/Codex outputs to managed native hook maps grouped by event and matcher, enforced conflict-safe regeneration semantics with regression coverage, and hardened atomic writes to clean temp files on failure without damaging existing targets (file index): `src/generate/mod.rs`, `src/runtime/fs.rs`, `tests/cli_generate.rs`, `AGENTS.md`.
- Phase 04 follow-up fixes: normalized native platform event names back into unified runtime events, added all-target preflight so generate cannot leave partial output on conflict, and raised workspace line coverage back above the 95% gate with targeted unit/integration tests (file index): `src/app.rs`, `src/generate/mod.rs`, `src/platform/capability.rs`, `src/run/mod.rs`, `tests/cli_generate.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Phase 04 protocol follow-up: preserved raw platform event names alongside normalized internal events so Codex responses keep native `event` values while rule matching still uses unified names, with regression coverage for native-event round trips (file index): `src/platform/codex.rs`, `src/platform/claude.rs`, `src/run/mod.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Phase 04 Codex protocol correction: replaced unsupported Codex block outputs with supported `decision/reason` responses on non-success paths and updated runtime/integration tests to assert the blocking protocol shape actually honored by Codex (file index): `src/platform/codex.rs`, `src/run/mod.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged changes, reran `cargo test --quiet`, and reported a Codex runtime event-name regression in generated-hook responses (file index): `src/run/mod.rs`, `src/platform/codex.rs`, `AGENTS.md`.
- Commit task: packaged the phase-04 managed generation, conflict-safe file writing, runtime raw-event preservation, and Codex protocol fixes into a single changeset (file index): `src/app.rs`, `src/generate/mod.rs`, `src/platform/capability.rs`, `src/platform/claude.rs`, `src/platform/codex.rs`, `src/run/mod.rs`, `src/runtime/fs.rs`, `tests/cli_generate.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Phase 05 runtime input parsing and normalized-context implementation: split Claude/Codex stdin parsing into platform-specific normalized context extractors, tightened required native fields and supported-event validation, preserved raw payload passthrough in runtime context, aligned run/CLI fixtures with native `session_id` payloads, and cleared fmt/clippy/test gates for the updated parsing path (file index): `src/platform/mod.rs`, `src/platform/claude.rs`, `src/platform/codex.rs`, `src/run/mod.rs`, `src/generate/mod.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Phase 06 command-execution and retry-state implementation: split `run` into retry-guard/execution/update helpers, stabilized internal `success|stop|block|error` outcomes, mapped non-zero child exits to block results while keeping spawn/timeout failures as errors, enforced bridge env injection over user env, and added unit/integration coverage for retry-state creation/isolation/cleanup plus execution request wiring (file index): `src/run/mod.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Coverage-gate follow-up: added targeted runtime IO/FS/process coverage for stdio-path error mapping, non-`NotFound` remove-file failures, tracking-FS invalid-data/missing-rename cases, and child-stdin write failures, restoring workspace llvm-cov line coverage above the 95% gate (file index): `src/runtime/io.rs`, `src/runtime/fs.rs`, `src/runtime/process.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged changes and reported a non-executing stdio regression test plus a non-hermetic `python3` dependency in process tests (file index): `src/runtime/io.rs`, `src/runtime/process.rs`, `AGENTS.md`.
- Coverage-test hermeticity fix: extracted `StdIo` read/write error mapping into production helpers so tests hit real implementation paths instead of re-stating `map_err`, and replaced the `python3`-based stdin-write failure test with a shell-only variant to keep process coverage portable across environments (file index): `src/runtime/io.rs`, `src/runtime/process.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged changes for the coverage-test hermeticity follow-up and found no blocking issues (file index): `AGENTS.md`.
- Module-length refactor: split oversized config/generate/run modules into focused schema/normalize, build/managed, and context/retry/test submodules without changing CLI behavior, preserving existing coverage and static-check outcomes (file index): `src/config/mod.rs`, `src/config/schema.rs`, `src/config/normalize.rs`, `src/config/tests.rs`, `src/generate/mod.rs`, `src/generate/build.rs`, `src/generate/managed.rs`, `src/generate/tests.rs`, `src/run/mod.rs`, `src/run/context.rs`, `src/run/retry.rs`, `src/run/tests.rs`, `AGENTS.md`.
- Code review task: reviewed current staged/unstaged changes for the module-length refactor and found no blocking issues (file index): `AGENTS.md`.
- Phase 07 platform output translation implementation: introduced platform-native `PlatformOutput` translation results, split Claude/Codex runtime response mapping into dedicated platform modules, aligned Codex event decision capabilities with translation behavior and retry-guard fallback semantics, updated run/CLI tests for empty-success stdout and native stop/block output shapes, and preserved full fmt/test/clippy gate coverage (file index): `src/platform/mod.rs`, `src/platform/capability.rs`, `src/platform/claude.rs`, `src/platform/codex.rs`, `src/run/mod.rs`, `src/run/tests.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged phase-07 translation changes, reran `cargo test --quiet` and `cargo clippy --all-targets --all-features -- -D warnings`, and found no blocking issues (file index): `AGENTS.md`.
- Phase 08 CLI composition and error-mapping implementation: extracted top-level parse/output orchestration into a testable library entrypoint, reduced `main` to thin emission logic, added stable CLI exit-code coverage for missing args/invalid platform/config validation/file conflict/JSON parse paths, and verified full fmt/clippy/test gates (file index): `src/lib.rs`, `src/main.rs`, `tests/cli_meta.rs`, `tests/cli_generate.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Phase 08 CLI error-format follow-up: preserved raw `clap` parse diagnostics on parameter failures while keeping stable exit-code mapping, tightened invalid-platform integration coverage to assert the native stderr prefix, and reran fmt/clippy/test gates cleanly (file index): `src/lib.rs`, `tests/cli_meta.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged phase-08 CLI composition/error-format changes, reran `cargo test --quiet`, and found no blocking issues (file index): `AGENTS.md`.
- Phase 09 integration-tests and coverage-gate closure: added CLI regression coverage for invalid YAML syntax, Claude generate-to-run native round trips, and retry-state consecutive-failure persistence, then introduced a single `make verify` local gate that runs fmt, clippy, and the enforced 95% coverage check (file index): `tests/cli_generate.rs`, `tests/cli_run.rs`, `Makefile`, `README.md`, `AGENTS.md`.
- Code review task: reviewed current unstaged phase-09 integration-test and verify-gate changes, reran `cargo test --quiet` and `make verify`, and found no blocking issues (file index): `AGENTS.md`.
- Code review task: reviewed current unstaged phase-09 integration-test and verify-gate changes again, reran `cargo test --quiet` and `make verify`, and found no blocking issues (file index): `AGENTS.md`.
- Example config task: added basic, platform-override, and retry/env YAML samples under `examples/` and updated the robot-maintained task index (file index): `examples/basic.yaml`, `examples/platform-overrides.yaml`, `examples/retry-and-env.yaml`, `AGENTS.md`.
- Hook parity gap implementation: expanded Claude/Codex capability matrices to native event coverage with alias compatibility, switched config normalization and generation to native event names with `SessionStart` matcher support, added bridge-stdout structured result parsing for additional-context/permission/worktree/elicitation flows, translated new event-specific runtime outputs for Claude and Codex, added unit/CLI regressions for extended events and structured protocols, and restored `make verify` coverage above the 95% gate (file index): `src/config/normalize.rs`, `src/config/tests.rs`, `src/generate/build.rs`, `src/generate/tests.rs`, `src/platform/capability.rs`, `src/platform/claude.rs`, `src/platform/codex.rs`, `src/platform/mod.rs`, `src/run/mod.rs`, `src/run/retry.rs`, `src/run/tests.rs`, `tests/cli_generate.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Hook parity regression fix: made structured bridge stdout conditional on zero child exit so non-zero commands still propagate failure/retry semantics, enforced Claude fallback translation through event-capability checks for side-effect-only events, updated Claude CLI regression to a block-capable event, and added unit/CLI coverage for non-zero structured stdout plus invalid fallback protocol prevention (file index): `src/run/mod.rs`, `src/run/tests.rs`, `src/platform/claude.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged hook-parity changes, reran `cargo test --quiet` and `make verify`, and reported a structured-output exit-status regression plus Claude side-effect-event failure translation issue (file index): `src/run/mod.rs`, `src/platform/claude.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged hook-parity follow-up changes, reran `cargo test --quiet`, and reported Claude permission-request `updatedInput` loss plus capability-matrix mismatches for `SessionStart`/`Elicitation` (file index): `src/platform/claude.rs`, `src/platform/capability.rs`, `AGENTS.md`.
- Hook parity Claude stop-hook follow-up: preserved `hookSpecificOutput.decision.updatedInput` for `PermissionRequest`, aligned Claude capability metadata so `SessionStart` rejects unsupported top-level decision fields while `Elicitation` and `ElicitationResult` allow matchers, and added config/generation/translation regressions covering those protocol edges before rerunning `cargo test --quiet` and `make verify` cleanly (file index): `src/platform/claude.rs`, `src/platform/capability.rs`, `src/config/tests.rs`, `src/generate/tests.rs`, `AGENTS.md`.
- Hook parity completion audit: cross-checked the phase 01-04 implementation docs against the current config normalization, generation, platform translation, runtime parsing, and regression coverage, and found no remaining implementation gaps after the hook-parity follow-up fixes (file index): `AGENTS.md`.
- Hook parity protocol follow-up: removed Claude `SubagentStop` from top-level block translation so it now respects the stop-only capability contract, rejected Codex `permission_decision.updated_input` with an explicit protocol error instead of silently dropping it, and added translator regressions before rerunning `cargo test --quiet` and `make verify` cleanly (file index): `src/platform/claude.rs`, `src/platform/codex.rs`, `AGENTS.md`.
- Hook parity Codex stdout follow-up: promoted zero-exit plain-text stdout on Codex `SessionStart` and `UserPromptSubmit` into `additional_context`, rejected plain-text success stdout for Codex `Stop` with an explicit protocol error, and added run/CLI regressions before rerunning `cargo test --quiet` and `make verify` cleanly (file index): `src/run/mod.rs`, `src/run/tests.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Hook parity Claude matcher/context follow-up: enabled matcher support for Claude `Notification`, `SubagentStart`, and `SubagentStop`, preserved `additionalContext` in `PermissionRequest` decision translation alongside `behavior` and `updatedInput`, and added config/generation/translation regressions before rerunning `cargo test --quiet` and `make verify` cleanly (file index): `src/platform/capability.rs`, `src/platform/claude.rs`, `src/config/tests.rs`, `src/generate/tests.rs`, `AGENTS.md`.
- Hook parity teammate-feedback follow-up: propagated platform-native `exit_code` and `stderr` through run/CLI output handling, translated Claude `TaskCreated`/`TaskCompleted`/`TeammateIdle` child `exit 2` into teammate feedback instead of protocol errors, and added translator plus CLI regressions before rerunning `cargo test --quiet` and `make verify` cleanly (file index): `src/platform/mod.rs`, `src/platform/claude.rs`, `src/platform/codex.rs`, `src/run/mod.rs`, `src/run/tests.rs`, `src/app.rs`, `src/lib.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Hook parity Claude block-semantics follow-up: mapped Claude `PermissionRequest` child `exit 2` into native deny-decision output instead of protocol errors, restored `SubagentStop` block semantics in the capability matrix and translator so stop-blocking paths no longer fail the bridge, and added translator plus CLI regressions before rerunning `cargo test --quiet` and `make verify` cleanly (file index): `src/platform/capability.rs`, `src/platform/claude.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Hook parity retry-state follow-up: validated platform translation before persisting retry outcomes so translate-time protocol failures now count as failures instead of slipping past retry protection, and added run/CLI regressions covering invalid structured output retry-state creation before rerunning `cargo test --quiet` and `make verify` cleanly (file index): `src/run/mod.rs`, `src/run/tests.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged hook-parity follow-up changes, reran `cargo test --quiet`, and found no blocking issues (file index): `AGENTS.md`.
- Code review task: reviewed current unstaged hook-parity changes, reran `cargo test --quiet`, and reported a Claude `SubagentStop` capability/translation mismatch that still emits block output plus a Codex `permission_decision.updated_input` silent-drop regression (file index): `src/platform/claude.rs`, `src/platform/capability.rs`, `src/platform/codex.rs`, `src/run/mod.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged hook-parity changes, reran `cargo test --quiet`, and reported a Codex plain-stdout handling gap where `SessionStart`/`UserPromptSubmit` additional context is dropped and `Stop` incorrectly accepts non-JSON success output (file index): `src/run/mod.rs`, `src/platform/codex.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged hook-parity follow-up changes, reran `cargo test --quiet` and `make verify`, and reported a Claude `SubagentStop` capability/translation regression plus missing exit-code-2 feedback semantics for `TaskCreated`/`TaskCompleted`/`TeammateIdle` (file index): `src/platform/capability.rs`, `src/platform/claude.rs`, `src/run/mod.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged hook-parity follow-up changes, reran `cargo test --quiet`, and confirmed a remaining Claude exit-code-2 regression for `TaskCreated`/`TaskCompleted`/`TeammateIdle` where non-zero hook exits now become protocol errors instead of documented feedback/block behavior (file index): `src/run/mod.rs`, `src/platform/claude.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged hook-parity follow-up changes, reran `cargo test --quiet`, and reproduced Claude `PermissionRequest`/`SubagentStop` exit-2 protocol regressions where blocking hook failures now surface as `platform protocol error` instead of event-native deny/stop behavior (file index): `src/platform/claude.rs`, `src/platform/capability.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged hook-parity follow-up changes, reran `cargo test --quiet` and `make verify`, and reported that invalid structured/plain-text bridge outputs now bypass retry-state persistence because `execute_rule` updates retries before platform output validation completes (file index): `src/run/mod.rs`, `AGENTS.md`.
- Code review task: reviewed current unstaged hook-parity follow-up changes, reran `cargo test --quiet`, and found no blocking issues (file index): `AGENTS.md`.
- Code review task: reviewed current unstaged hook-parity follow-up changes, reran `cargo test --quiet`, and found no blocking issues in the native-event normalization, bridge-output parsing, and Claude/Codex translation follow-up pass (file index): `src/app.rs`, `src/config/normalize.rs`, `src/generate/build.rs`, `src/lib.rs`, `src/platform/mod.rs`, `src/platform/capability.rs`, `src/platform/claude.rs`, `src/platform/codex.rs`, `src/run/mod.rs`, `src/run/retry.rs`, `src/run/tests.rs`, `tests/cli_generate.rs`, `tests/cli_run.rs`, `AGENTS.md`.
- Example config refresh task: rewrote the basic, platform-override, and retry/env example YAMLs to match the current event/capability model, added a Claude extended-events sample covering `PermissionRequest`/`Elicitation`/`SubagentStop`/`TeammateIdle`, and updated the robot-maintained task index (file index): `examples/basic.yaml`, `examples/platform-overrides.yaml`, `examples/retry-and-env.yaml`, `examples/claude-extended-events.yaml`, `AGENTS.md`.
- Example config review fix: removed relative `working_dir` overrides that changed event-cwd semantics and split retry/env payload capture into separate payload and metadata files so the saved payload stays intact (file index): `examples/basic.yaml`, `examples/retry-and-env.yaml`, `AGENTS.md`.
- Example config review fix: made the pre-tool audit sample append payload history instead of overwriting prior entries, and changed the cwd-override sample to use a fixed absolute `working_dir` so it actually demonstrates directory override semantics (file index): `examples/basic.yaml`, `examples/retry-and-env.yaml`, `AGENTS.md`.
- Code review task: reviewed current unstaged example-config refresh changes and reported payload-file corruption plus relative-working-dir semantic regressions in the new YAML samples (file index): `examples/basic.yaml`, `examples/retry-and-env.yaml`, `AGENTS.md`.
- Example config review fix: removed unsupported static Codex `PreToolUse` stop fields from the dangerous-command sample and changed retry/env audit capture to append payload and metadata histories instead of overwriting prior runs (file index): `examples/platform-overrides.yaml`, `examples/retry-and-env.yaml`, `AGENTS.md`.
- Example config follow-up: removed the relative default `working_dir` from the Claude extended-events sample and added a dedicated `Stop` hook example covering Claude-only and Codex-only stop flows (file index): `examples/claude-extended-events.yaml`, `examples/stop-hooks.yaml`, `AGENTS.md`.
- Example config review fix: moved Claude `TeammateIdle` feedback to `stderr` so exit-2 feedback is visible, and removed the unsupported `system_message` field from the Claude `Stop` sample so the example matches actual translation behavior (file index): `examples/claude-extended-events.yaml`, `examples/stop-hooks.yaml`, `AGENTS.md`.
- Code review task: reviewed current unstaged example-config follow-up changes, reran `cargo test --quiet`, and found no blocking issues (file index): `AGENTS.md`.
- Code review task: reviewed current unstaged example-config follow-up changes, reran `cargo test --quiet`, and reported that the new Claude extended-events sample reintroduced a relative default `working_dir` that overrides the incoming event cwd and undermines the documented example semantics (file index): `examples/claude-extended-events.yaml`, `AGENTS.md`.
- Code review task: reviewed current unstaged example-config follow-up changes, reran `cargo test --quiet`, validated all updated example configs with `cargo run -- generate --config` in an isolated temp workspace, and found no blocking issues (file index): `examples/basic.yaml`, `examples/platform-overrides.yaml`, `examples/retry-and-env.yaml`, `examples/claude-extended-events.yaml`, `examples/stop-hooks.yaml`, `AGENTS.md`.
- Hook config migration task: converted the repo-local Codex stop hooks into a shared `hook_bridge` YAML source, generated managed Codex and Claude hook files from that config, replaced the static-check Python hook with a shell gate under `scripts/hooks`, and switched automated review hook JSON emission to the `hook_bridge` protocol before rerunning `cargo run -- generate --config hook-bridge.yaml` and `cargo test --quiet` cleanly (file index): `hook-bridge.yaml`, `scripts/hooks/static_checks.sh`, `scripts/hooks/auto_review.sh`, `.codex/hooks/auto_review.py`, `.codex/hooks.json`, `.claude/settings.json`, `AGENTS.md`.
- Code review task: reviewed current unstaged hook-config migration changes and reported a Claude auto-review state-collision regression plus relative hook-command path breakage outside the repo root (file index): `hook-bridge.yaml`, `.codex/hooks/auto_review.py`, `src/run/mod.rs`, `AGENTS.md`.
- Auto-review hook migration fix: restored the deleted automated review Python implementation from git into `scripts/hooks`, moved its prompt/schema companions alongside it, updated the shell wrapper to execute the new stable path under the repo root, and kept the `.codex/hooks` removal intact (file index): `scripts/hooks/auto_review.py`, `scripts/hooks/review_prompt.md`, `scripts/hooks/review_schema.json`, `scripts/hooks/auto_review.sh`, `AGENTS.md`.
- Static-check hook simplification: inlined the `make verify` block response directly into the shared hook config and removed the redundant `scripts/hooks/static_checks.sh` wrapper (file index): `hook-bridge.yaml`, `scripts/hooks/static_checks.sh`, `AGENTS.md`.
